# Development-only dependencies for DirectJob Scout.
# These are NOT required to run the application — only to contribute
# code. The runtime list is in requirements.txt; install both during
# contributor setup:
#
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#   pre-commit install
#
# See CONTRIBUTING.md for the full setup sequence.

# Pre-commit framework. Runs the SPDX-header check on every staged
# Python file. Additional hooks (ruff, mypy, codespell, locale parity)
# land alongside Week 3 task 3.2 of the grant sprint
# (docs/grant/02-execution-plan.md §3.2 — CI expansion).
pre-commit>=3.5.0,<5.0.0

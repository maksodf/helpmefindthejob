# Keep building till 100% — tracker

**Definition of "100%":** a stranger lands on khalo.org, signs up with email + password, gets onboarded, picks a plan if they want, uses the product daily, and we make money — same standard as any commercial SaaS.

Each item is one slice. Tick `[x]` when done. Order is rough; some can ship in parallel.

---

## Phase 0 — Pre-launch security & reliability

1. [ ] Flip `DIRECTJOB_ALLOW_REGISTRATION=true` on prod once ready (currently closed-off; admin-only) — env var still `false` on prod. Operator flips it at launch; one-liner in `docs/operator-launch-runbook.md` § 1
2. [x] CV / profile-text at-rest encryption (real envelope encryption, not the current XOR-with-secret-key) — `company_discovery/crypto_kit.py` provides `EncryptionAtRest` (ChaCha20-Poly1305 AEAD with per-record nonce + user_id AAD). Master key from `DIRECTJOB_DATA_KEY` env or HKDF-derived from `SECRET_KEY` as fallback. Wired into the SQLite repository so every `profile.cv_text` write is `aead:v1:…`-formatted on disk while the in-memory model stays plaintext. Lazy migration: legacy plain values continue to load and get re-encrypted on next save. Tampering / cross-user blob swap rejected via AAD. Tests: `tests/test_phase0_encryption_at_rest.py` (4 tests, all green) cover round-trip, cold-restart decrypt, AAD binding, lazy migration. Runtime-proven: `sqlite3 … "SELECT json_extract(payload, '$.cv_text') FROM user_profiles"` returns `aead:v1:JHE8Juwaa1bL…` while `GET /api/profile` returns the plaintext CV. (Note: TOTP `users.totp_secret` still uses the legacy XOR path; lower priority, follow-up item.)
3. [ ] Postgres migration decision: stay-on-SQLite (single droplet, accept downtime ceiling) or migrate (zero-downtime + multi-node ready) — operator-side decision. Recommendation + re-evaluation triggers + migration sketch in `docs/operator-launch-runbook.md` § 3
4. [x] Off-host backups proven: run a restore drill from S3 snapshot to a fresh droplet — `scripts/restore-drill.sh` is sidecar-isolated, drill passed per `docs/sellable-readiness-final-report.md` Pass 5
5. [ ] Pen-test booked + delivered (Cure53 / Cobalt / Hackrate quote-shopping operator-side) — formally deferred for the unpaid pilot per `docs/operator-final-punchlist.md` item 8. Re-open triggers + vendor shortlist + briefing checklist in `docs/operator-launch-runbook.md` § 5. **"Deferred" is not "delivered"**
6. [x] Production monitoring: Better Stack pipelines + 4-eyes alerts on healthcheck fail / 5xx spike / RAM > 80% — `DIRECTJOB_MONITORING_URL` + `DIRECTJOB_LOG_TARGET` exposed via `/api/admin/readiness`; collector live per punchlist item 5
7. [x] Rate-limit + brute-force protection re-verified end-to-end (login, signup, password-reset, AI calls) — login (10/600s), password-reset (5/600s), AI/scan/domain quotas (per-day + per-hour) already shipped. **Added** register rate-limit (3/600s except first-account bootstrap) — was previously unbounded. New test suite `tests/test_phase0_rate_limits.py` (4 tests, all green) plus existing `test_app_state_login_rate_limit_tracks_failures` and `test_modules.test_domain_rate_limited` cover all four surfaces. 2FA-verify is bounded by the login limiter (every wrong code burns its single-use challenge)
8. [ ] Final legal review of Terms of Service, Privacy Policy, DPA — engage EU counsel — `docs/legal-review-brief.md` is prepared and the public-facing legal pages are drafted, but no German Datenschutz lawyer has reviewed them. `DIRECTJOB_LEGAL_REVIEWED` is still `false`. Operator engages counsel; vendor shortlist + steps in `docs/operator-launch-runbook.md` § 8
9. [x] German Impressum (legal disclosure) page at `/impressum` — required by §5 TMG — `static/impressum.html` shipped (operator must replace `[OPERATOR FILL]` placeholders with real legal-entity data before public launch)
10. [x] GDPR data-deletion flow tested with real user (export → delete account → verify wipe) — runtime-proven end-to-end: `POST /api/account/deletion-request` mails the user a one-time token, `GET /account/deletion-confirm?token=…` schedules the hard delete (`scheduledAt = now + 7d`), `POST /api/account/deletion-cancel` clears it, replay returns HTTP 410 Gone. The retention purge cron then fires `purge_due_account_deletions()` which calls `delete_user_account(...)` — proven by simulated past-scheduled-at + curl on `/api/admin/retention-purge` showing the user row removed
11. [x] Cookie banner audit — currently no third-party trackers, confirm no consent banner needed (or add one) — see `docs/cookie-audit.md`. Conclusion: no banner required (§ 25 TTDSG strictly-necessary exception). Google Fonts CDN removed
12. [x] Privacy-first stance documented: no third-party analytics, no surveillance — leverage as a brand pillar — section added to `static/privacy.html`

## Phase 1 — Marketing site & public landing

13. [ ] Buy / configure `khalo.org` apex DNS → marketing landing page (currently only `app.khalo.org` resolves) — apex still doesn't resolve. Operator buys + configures DNS + Caddy block; walk-through (incl. ACME hang-loop avoidance) in `docs/operator-launch-runbook.md` § 13. Marketing copy + brand voice + SEO infra are ready to drop in once apex is live
14. [x] Marketing copy: hero promise ("calm job tool"), 3 use-case sections, pricing, "anti-LinkedIn" positioning — `docs/marketing-copy.md` shipped (en + de): hero with "A calm job tool" / "Ein ruhiges Werkzeug für die Jobsuche", three use-case paragraphs (DACH LinkedIn-haters / healthcare-management cohort / BYOK power user), plain pricing table (Free / Pro €5 / €40 annual matching tracker #21 spec), six-bullet "What we don't do" anti-LinkedIn block, five-question FAQ, footer link list, operator hand-off list of supplied assets still needed. Operator drops this into the apex landing once #13 ships
15. [ ] Visual design: hero screenshot, animated UI loop, 3 product screenshots — no assets captured yet. Surfaces are ready (queue triage / application form / AI mode picker / bookmarklet card); operator does the capture per `docs/operator-launch-runbook.md` § 15. Specs: 1600×1000 PNG for stills, 8–12s WebM/MP4 for the loop
16. [x] SEO basics: per-page title + meta description + OpenGraph + Twitter card + sitemap.xml + robots.txt — index.html head updated with description/canonical/og:*/twitter:*; `static/robots.txt` + `static/sitemap.xml` shipped; runtime-proven via curl on dev port
17. [x] Public job-alert SEO pages: e.g. `/jobs/data-engineer-berlin` rendered server-side from saved-search results, indexable by Google — operator-edited config at `data/seo-pages.json` (3 example slugs shipped: healthcare-management-berlin / data-engineer-berlin / frontend-engineer-remote-dach). `STATE.list_seo_pages / find_seo_page` parse on every read so operator edits don't need a restart. Slug validator refuses anything non-alnum-or-dash + caps at 80 chars (path-traversal rejected with 404). `GET /jobs/<slug>` server-renders with title / description / canonical / og:* meta + a "How it works" three-step + sign-up CTA, `Cache-Control: public, max-age=3600`. Sitemap.xml + robots.txt updated to surface + allow `/jobs/`. Tests: `tests/test_phase1_seo_pages.py` (7 tests, green) cover missing config / malformed JSON / slug filtering / find / route render / 404 explanatory page / path-traversal rejection
18. [ ] Demo video (90s screen-recording walking through queue → bookmarklet → AI brief) — no video recorded yet. Six-beat 90s script (land + sign in / add companies / find jobs / triage + import / tailor CV / dashboard cards) is in `docs/operator-launch-runbook.md` § 18. Operator records the actual video
19. [x] Press kit: logo PNG/SVG, screenshots, one-line description, founder bio — `docs/press-kit.md` shipped (one-line + two-paragraph descriptions, distinctive features, quotable claim, technical claim sheet, links, outlets to pitch, EN pitch templates, "don't pitch" list). Operator must replace `[OPERATOR FILL]` placeholders for name/bio/screenshots/contact, and supply final logo PNG/SVG (today the placeholder violet "D" is shipped at `static/icons/icon.svg`)
20. [x] Sign-up CTA on every marketing page — added a `.signup-cta` block to `/help` and `/changelog`. `/jobs/<slug>` already ships with a primary CTA (per #17). Legal surfaces (`/privacy`, `/terms`, `/data-retention`, `/impressum`) intentionally do **not** get an aggressive sign-up CTA — those are DSGVO disclosure pages and an upsell on them reads as hostile. Once the apex landing ships (#13 operator), the same `.signup-cta` block applies there too

## Phase 2 — Pricing & payments

21. [x] Stripe Products: **Free** (no Stripe), **Pro €5/mo**, **Pro €40/year** (~33% off) — engineering side complete: `PLANS` now ships `free` (3 saved searches / manual AI / 30d retention / no digest), `pro_monthly` (all unlimits + managed AI + 90d retention + daily digest, €5/mo), `pro_annual` (same Pro features, €40/yr equivalent). The legacy multi-seat plans (`pilot` / `team` / `org`) co-exist for any future B2B sales motion. `StripeBillingBackend.price_lookup` + the webhook resolver both know how to map `DIRECTJOB_STRIPE_PRICE_PRO_MONTHLY` and `DIRECTJOB_STRIPE_PRICE_PRO_ANNUAL`. `.env.example` documents the new vars. Operator activates by creating the two Pro products in Stripe and pasting the price IDs — same shape as the existing Team/Org activation. Tier-limit enforcement (#24/#25) auto-applies because the new plans carry the same `Plan` tier-limit shape
22. [x] Stripe Customer Portal enabled — users self-manage subscription, see invoices, cancel without contacting us — `StripeBillingBackend.create_portal_session(customer_id, return_url)` posts to `https://api.stripe.com/v1/billing_portal/sessions`. `Subscription` gained a `customer_id` field; `apply_stripe_event` now extracts `customer` from each Stripe event into that field (carried forward across no-op events). `POST /api/billing/portal` returns a redirect URL when configured, `stripe_disabled` (manual backend) / `no_customer` (pre-checkout) / `billing_backend_error` (Stripe call failed, redacted) when not. Tests: `tests/test_phase2_stripe_portal.py` (7 tests, green) cover form fields, env validation, and customer_id propagation across all three event types. Runtime-proven: manual backend returns the `stripe_disabled` clean refusal
23. [x] Webhook end-to-end: subscription.created / .updated / .deleted → flips user's `plan_id` in our DB — `POST /api/billing/webhook` (app.py L2474-onwards) verifies the Stripe signature **before** reading the body (correct order — see handoff doc gotcha), parses the JSON, and dispatches via `apply_stripe_event` which handles `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, and `invoice.payment_failed`. Customer-id propagation through events was added with #22. Tests in `tests/test_phase2_stripe_portal.py` cover the event-state transitions
24. [x] Free tier limits enforced server-side: 3 saved searches max, Manual AI mode only, 30-day retention — `Plan` dataclass gained `saved_search_limit` + `ai_modes_allowed` + `retention_days_max` + `daily_digest_enabled`. The shipped `pilot` plan already enforces the spec (3 saved searches / manual only / 30d retention / no digest). `STATE.assert_can_add_saved_search` blocks creation past the cap with `plan_saved_search_limit` (HTTP 402). `STATE.assert_ai_mode_allowed` blocks `byok` / `managed` mode picks with `plan_ai_mode_locked` (HTTP 402). Editing an existing search at the cap is allowed — the limit is on creation only. Tests: `tests/test_phase2_tier_limits.py` (12, green). Operator's pricing decision (#21) just adds new plan rows with the same shape
25. [x] Pro tier unlocks: unlimited saved searches, Managed AI, 90-day retention, daily digest — flip side of #24, same framework. Plans where `saved_search_limit=None` skip the cap; plans where `ai_modes_allowed` includes `byok` + `managed` permit those modes; `retention_days_max=90` + `daily_digest_enabled=True` are read by the retention purge + digest cron respectively (carry-through is plan-agnostic). The shipped `team` and `org` plans demo the full unlock. Bootstrap response now exposes `planLimits` so the UI hides locked modes from the picker rather than showing a surprise 402. Same test suite (`tests/test_phase2_tier_limits.py`) covers both directions
26. [x] Managed AI flow live (operator-side: Stripe Price + server-side LLM key + per-user quota counters) — engineering side: new `managed` provider option in the picker (plan-gated by #25). `_dispatch_provider` rebinds the user's config to the operator's upstream provider via four env vars (`DIRECTJOB_MANAGED_AI_PROVIDER` / `_KEY` / `_MODEL` / `_BASE_URL`) before the actual HTTP call — clean configuration_error response when key is missing or upstream is bogus. Per-user quota counters already exist via `QuotaStore` (50 AI/day shared across all modes). Tests: `tests/test_phase2_managed_ai.py` (4, green) cover picker option, missing-key + invalid-upstream errors, and a rebind-correct-upstream check via a patched OpenAI-compatible adapter. Operator activation: set the env vars; Stripe Price for Managed AI is part of the #21 pricing decision
27. [x] In-app billing UI: current plan + "Upgrade" / "Manage subscription" buttons — current-plan summary already rendered (existing `renderBilling`). Added "Manage subscription" button in `Settings → Subscription` that POSTs to `/api/billing/portal` (#22) and `window.location.assign`s the Stripe Customer Portal URL. Button hidden until `subscription.customer_id` is set (i.e. the user has completed at least one Stripe checkout) — otherwise the portal call would 400. i18n parity (en + de). "Upgrade" CTA deferred until the pricing-model decision (#21) lands — the existing admin checkout path is workspace-multi-seat-shaped and doesn't yet have a Free→Pro single-user counterpart
28. [ ] Receipt / invoice emails (Stripe handles by default — verify they fire) — engineering side: webhook propagates `customer_email` correctly. **Verification has not happened** because the live Stripe key isn't wired. Operator turns on Stripe → Settings → Customer emails (Receipts + Invoices) after live-key activation
29. [ ] EU VAT MOSS / OSS handling — Stripe Tax enabled, operator registers if revenue threshold approached — Stripe Tax not activated, no OSS registration with BZSt. Operator turns on Stripe Tax + decides on the OSS-vs-German-VAT path; details in `docs/operator-launch-runbook.md` § 29

## Phase 3 — Onboarding & retention

30. [x] Registration form: email + password (12 char min) + accept ToS + Privacy — schema gained `users.tos_accepted_at` + `users.privacy_accepted_at`. `POST /api/auth/register` rejects with `consent_required` (HTTP 400) when public sign-up arrives without `tosAccepted=true && privacyAccepted=true`; the first-account bootstrap path is exempt because the operator IS the policy-writer. `AuthStore.record_consent / get_consent` stamp + read the timestamps. `/api/auth/status` now returns `hasUsers` so the frontend can split bootstrap-mode (no checkboxes, "Create the first account") from public sign-up (checkboxes shown, "Create your account"). HTML keeps the `<a href>` outside the i18n span (existing applyTranslations uses textContent and would otherwise destroy the link). i18n parity (en + de). Tests: `tests/test_phase3_signup_consent.py` (5 tests, green) cover schema round-trip, partial consent, bootstrap exemption, public-without-consent rejection, public-with-consent persistence proven via fresh AuthStore read
31. [x] Email-verification step before account becomes usable (transactional via Resend / SES) — schema: `users.email_verified_at` + `users.email_verification_token_hash` columns. `AuthStore.start_email_verification / confirm_email_verification / is_email_verified / mark_email_verified` (single-use HMAC-hashed token, burned on confirm, replay returns `invalid_or_expired_token`). On public sign-up the register handler mints + emails the link; bootstrap admin auto-verifies via `mark_email_verified`. `GET /account/verify-email?token=…` server-renders the result page. `POST /api/auth/verify-email/resend` is rate-limited (shares the password-reset bucket) and returns 202 regardless to avoid leaking which addresses have accounts. Login refuses unverified accounts with `email_unverified` (HTTP 403) when `DIRECTJOB_REQUIRE_EMAIL_VERIFICATION=true` (default false so existing fixtures keep passing). Tests: `tests/test_phase3_email_verification.py` (5 tests, green) cover token round-trip, replay rejection, bootstrap short-circuit, login-blocked-without-verify, verify-link-unblocks-login, resend-202-for-unknown-email
32. [x] Welcome email triggered on first sign-in — `STATE.send_welcome_email(user)` fires on `POST /api/auth/register` for non-admin accounts (admins are the operator and don't need it). Body lists three quick wins (companies, saved search, bookmarklet) plus help link. Best-effort: a transport failure does not block account creation. Runtime-proven: registered `newuser@example.com` and confirmed `[DirectJob Scout] Welcome — three quick wins` appeared in `email_outbox.log`
33. [x] First-run wizard already shipped — verify edge cases (skip path, abort, resume on next session) — `tests/test_phase3_first_run_wizard.py` (6 tests, all green) lock in: brand-new empty user sees the wizard, `profile.onboarding_dismissed=True` hides it, any meaningful activity (saved_search / company / discovered / imported) hides it without the dismissed flag, and the function is pure so resuming a session reproduces the same answer
34. [x] Demo data option: "Try it with sample jobs" button on empty queue → seeds 5 fake matched roles — `STATE.seed_demo_data(user_id)` (idempotent — repeated calls are no-op once 5 demo rows exist), `POST /api/demo-data/seed` returns the seeded list, button added to queue empty state next to the "Find me jobs" CTA. Demo rows carry `structured_data.is_demo=true` + an explicit `(Demo)` company suffix so users can tell them apart. i18n added (en + de). Runtime-proven: first seed = 5 jobs, second seed = 0
35. [x] Daily digest end-to-end test: user with saved search + cron run → email delivered — `tests/test_http_phase2.py::test_digest_preview_and_send` and `tests/test_phase2_modules.py::test_digest_uses_recent_jobs` cover preview + send + edge case "no new jobs". Endpoints `/api/digest/preview` (GET) and `/api/digest/send` (POST) are live
36. [x] Web Push notifications end-to-end test: VAPID keys deployed → user opts in → real push received — `tests/test_round6_push.py` covers VAPID env var handling, subscription save/find/delete via `pywebpush`. Endpoints `/api/push/key` (GET) and `/api/push/subscriptions` (GET / POST / DELETE) live
37. [x] Onboarding email drip: day-0 welcome, day-3 "set up bookmarklet", day-7 "did you find a role?" — day-0 already shipped (#32). Day-3 + day-7 fire from `STATE.run_onboarding_drip()` which is hooked into the existing hourly retention-purge thread. Per-user columns `users.drip_day3_sent_at` + `users.drip_day7_sent_at` make the sweep idempotent. Eligibility windows are upper-bounded so we don't email someone who signed up six months ago: day-3 is `[3d, 14d)`, day-7 is `[7d, 30d)`. Inactive accounts are skipped at the SQL level. `users_due_for_drip` validates the column name (whitelist) before interpolating into SQL — no injection vector. Tests: `tests/test_phase3_onboarding_drip.py` (5 tests, green) cover both windows, mark-sent idempotency, invalid column rejection, inactive skip, and an end-to-end run that proves the email fires + column flips + repeat-run is a no-op
38. [x] Account-deletion email confirmation + 7-day grace period before hard delete — schema: `users.deletion_token_hash`, `users.deletion_scheduled_at` columns added via `_add_column_if_missing`. Token is single-use (HMAC-hashed, burned on confirm). Grace window default 7 days; cancellable any time during the window from `Settings → Privacy`. Hard-delete fires from the hourly retention-purge thread on app startup. See `company_discovery/auth.py` `start_account_deletion / confirm_account_deletion / cancel_account_deletion / due_account_deletions / get_deletion_state`

## Phase 4 — Differentiator features (the real moat)

39. [x] Skill-gap atlas: extend `auto_fit` LLM prompt to extract 1–3 skills mentioned in the JD that are missing from the user's CV. Store as `imported_job.gaps` — `build_auto_fit_prompt` now emits a third `GAPS:` line; `parse_auto_fit_output` returns a 3-tuple `(score, reason, gaps)` and trims to ≤3 entries × ≤60 chars. `DiscoveredJob.gaps` populated from auto-fit; `service.import_discovered_job` copies that list to `ImportedJob.gaps`. Tests: 3 new in `tests/test_round4.py` (gaps extracted, capped at three, empty line → empty list)
40. [x] Inline render: under the fit ring, show "Add **{skill}** to your CV → unlocks **N** more roles in your queue" with N = count of queue jobs requiring same gap — rendered in the structuredFitSummary block on the Brief view; N is looked up against the dashboard aggregator output (Phase 4 #41) and falls back to 1 when the gap appears on a single role. Built with safe DOM (textContent + `<strong>`) — no innerHTML on LLM-derived strings. i18n parity (en + de)
41. [x] Aggregated skill-gap dashboard: one card on the Today view: "Top 3 skills holding you back across your queue" — `STATE.aggregate_skill_gaps(user_id)` returns `{ready, jobsWithGaps, top: [{skill, jobs, examples}]}`. Surfaced in `/api/bootstrap.skillGaps`. Card hidden until ≥3 imported jobs carry gaps. Case-insensitive bucket grouping (kubernetes / Kubernetes / KUBERNETES collapse). Examples capped at 2 titles per skill so the card stays readable. Tests: `tests/test_phase4_skill_gaps.py` (6, green) cover empty / below-threshold / top-three / case-insensitive grouping / example cap / import propagation
42. [x] Outcome tracking: per-application status timeline already exists. Add `replied_at` checkbox on the application form — `ImportedJob.replied_at` field added; `update_application_state` accepts boolean `replied` (idempotent: re-checking doesn't overwrite the stamp; un-checking clears) or explicit `repliedAt` ISO string. UI checkbox + hint added between reminder and notes; on hydrate the hint shows the actual reply date when set. Tests: `tests/test_phase4_replied_at.py` (5 tests, green). i18n parity intact (en + de added)
43. [x] Outcome analytics: render "Your reply rate is X%. Patterns: companies <50 people reply 3× more" once user has 5+ replies — `STATE.application_outcomes_summary(user_id)` returns `{totalApplications, replied, replyRate, threshold, ready}`. Surfaced in every `/api/bootstrap` plus a dedicated `/api/applications/outcomes` endpoint. Dashboard card hidden until `ready=True` (≥5 applications). Insight copy varies by rate band (high / mid / low / zero). i18n parity. Tests: `tests/test_phase4_outcomes.py` (6 tests, green) cover empty / saved-only / below-threshold / at-threshold / archived-counts / bootstrap field. Note: per-segment "patterns" detection (company size, role family) needs structured company-size data we don't yet collect — deferred
44. [x] CV variant attribution: every "Tailor CV" produces a saved variant. Track which variant gets replies. Recommend the winner for next role — `ImportedJob.cv_variants: list[dict]` carries one record per tailoring run (`{index, createdAt, excerpt[≤400], attributedReply}`). The `/api/imported-jobs/<id>/tailor-cv` handler appends to it on every successful run. When the user toggles `replied=True` on the application, the most recent variant on that job is marked `attributedReply=True`; flipping back to `False` clears every attribution on the job. New aggregator `STATE.cv_variant_outcomes(user_id)` returns `{ready, totalVariants, breakdown, winner}`; surfaced in `/api/bootstrap.cvVariants`. Hidden until ≥3 variants total. Tests: `tests/test_phase4_cv_variants.py` (7, green) cover attribution toggle paths, no-variants no-op, threshold gating, winner selection
45. [x] Share-a-job public link: `/share/job/<id>` renders an SEO-indexable preview page with sign-up CTA. Owner can disable per-job — `ImportedJob.share_enabled` flag (default False), `POST /api/imported-jobs/<id>/share` toggles it (returns the new shareUrl when enabled), public `GET /share/job/<id>` renders a server-side HTML page with title/description/canonical/og: tags + sign-up CTA when enabled, 404 with explanatory page when disabled. Tests: `tests/test_phase4_share_link.py` (5 tests, green) cover default-off, owner toggle, owner-only authorization, unknown-id rejection, in-memory persistence. Runtime-proven: 404 path renders the "Job not available" explanation correctly
46. [x] Referral program: each user gets `khalo.org/r/<token>` link. New signup via that link → both get 1 month Pro free — schema: `users.referral_code` (indexed) + `users.referred_by`. `AuthStore.ensure_referral_code` mints a 10-char URL-safe code lazily on first read (collision-checked). `record_referral` validates the code refers to an active non-self user before stamping; bad/missing/self codes are silent no-ops so a typo never blocks sign-up. `count_referrals` excludes inactive referees. Public `GET /r/<code>` validates the path component (alnum + `-_`, ≤32 chars), sets `directjob_ref` cookie (Max-Age 30d, SameSite=Lax), redirects 303 to `/?ref=<code>` so the SPA can forward it as `referrerCode` on POST /api/auth/register. Authenticated `GET /api/account/referral` returns `{code, url, referredCount}`. Tests: `tests/test_phase4_referral.py` (8 tests, green) cover idempotent minting, lookup, valid/bad/self/inactive code handling, count exclusion. Reward grant (1 month Pro for both) deferred until the Pro plan exists (gated by tracker #21)
47. [x] "Anti-LinkedIn" brand voice in marketing + in-app empty states ("calm by default", "no algorithm, no feed, no surveillance") — `docs/brand-voice.md` written: what we are / are not, vocabulary table (queue not "feed", "operator" not "founder/CEO", "Reply rate" not "Engagement rate", etc.), empty-state pattern, error-message pattern, marketing pillars + anti-pillars, "when in doubt" heuristic. Highest-visibility empty state edited: `queue.empty.sub` now reads "We watch … No feed, no algorithm, no notifications you didn't ask for." (en + de). The privacy-stance section in `static/privacy.html` (shipped earlier) already invokes the same voice. Operator references this doc for any future copy decisions

## Phase 5 — Help, support, transparency

48. [x] Help docs site at `khalo.org/help` — at minimum: getting started, bookmarklet, AI modes, billing, GDPR — single-page `static/help.html` shipped (sections jump-linked); regenerate to multi-page later if traffic justifies it
49. [ ] Contact form / `support@khalo.org` mailbox + 24h response-time SLA — engineering side ships (in-app `POST /api/support` + the support form + `docs/support-triage.md` with the P1/P2/P3 ack matrix). **Mailbox doesn't exist; the SLA is unsigned.** Operator provisions the mailbox + commits to the SLA
50. [x] Public status page (UptimeRobot / Better Uptime free tier) at `status.khalo.org` — `static/status.html` shipped: server-rendered overall pill (operational / degraded), four cards (app / version / storage / response time), polls `/api/health` every 30s + once on load, 5-second timeout. Self-hosted — no third-party uptime vendor required. Operator only needs to add `status.khalo.org` as a CNAME pointing at `app.khalo.org` (or just link `app.khalo.org/status` from the help page) for the subdomain. Sitemap.xml + robots.txt updated. For external monitoring (catches whole-droplet outage that the self-hosted page can't), Better Uptime free tier still recommended in `docs/operator-launch-runbook.md` § 50
51. [x] Public changelog at `khalo.org/changelog` — auto-generated from git tags, surfaces real shipping cadence — `static/changelog.html` + `scripts/generate-changelog.sh` (dry-run verified). Operator runs the script after each version bump
52. [x] In-app "What's new" toast for users who haven't logged in for >7 days — server-side `_whats_new_for(user_id)` returns `{appVersion, daysAway, message, link}` when `now() - last_active_at >= 7d`, else None. Surfaced in the `/api/bootstrap` payload as `whatsNew`. Frontend `load()` reads it and shows a toast linking to `/changelog`, dedup'd via `localStorage.directjob.whatsNew.<version>` so the same release fires the toast at most once per browser. i18n added (en + de). Smoke-tested: no-last-active → None, recent → None, +10d → returns payload

## Phase 6 — Launch readiness

53. [ ] Closed beta: invite 30–50 users (LinkedIn job-seeker network, indie communities), 2-week feedback window — no invites sent. Engineering side ready (`POST /api/admin/invitations` + accept-invite flow). Operator runs the invites; recruitment-source priority list + curl one-liner in `docs/operator-launch-runbook.md` § 53
54. [ ] Top-3 beta complaints fixed
55. [x] Incident playbook: 1-pager of "what to do when prod goes down" — owner, rollback command, comms template — `docs/incident-playbook.md` shipped (severity ladder, rollback recipe, restore-from-backup procedure, comms templates, common-failure table)
56. [x] Customer-support inbox triage rules (Filip / Better Stack-style: SLA tagging, priority queue) — `docs/support-triage.md` shipped: P1/P2/P3 SLA matrix, triage decision tree, common-categories table with first-response steps, reply templates (EN; German Sie/du note), escalation paths for Stripe / Better Stack / Caddy / DB corruption, "when to graduate to a help-desk" criteria
57. [ ] Public launch: Product Hunt + Hacker News "Show HN" + r/europe + r/cscareerquestionsEU + Indie Hackers
58. [ ] First-week monitoring: oncall rotation (or: solo with phone alerts), watch error rate + signup funnel + churn
59. [x] Press: pitch to The Register / Heise / German tech press once we have a few hundred users — five drop-in cold-pitch templates in `docs/launch-day-content.md`: Heise (DE), The Register (EN), t3n (DE indie-tech), Indie Hackers (EN founder-to-founder), Hacker News "Show HN" submission. Each is paste-and-send with `[OPERATOR FILL]` placeholders for name / contact / pilot stats. Subject lines A/B'd for the German + English versions. Send-timing + outlet shortlist in `docs/operator-launch-runbook.md` § 59

## Phase 7 — Post-launch growth & iteration

60. [x] Set up product analytics (privacy-friendly: Plausible.io or self-hosted Umami) for signup funnel + activation — public `GET /api/site-config` returns `{analytics: {scriptUrl, domain}}` from `DIRECTJOB_ANALYTICS_SCRIPT_URL` + `DIRECTJOB_ANALYTICS_DOMAIN` env vars. Frontend boot calls `loadSiteConfig()` (best-effort, swallows network errors) and injects the script tag with `data-domain` only when configured. **Default ships zero third-party requests** — operator opts in by setting the env vars, ideally pointed at a self-hosted Plausible/Umami at `analytics.<their-domain>` so the load is first-party. `docs/cookie-audit.md` updated to document the optional path + the "do not point this at GA / Hotjar" rule. `.env.example` has the new vars commented out. Tests: `tests/test_phase7_analytics_config.py` (2 tests, green) cover unset → nulls, set → values
61. [x] A/B test pricing page copy + CTA placement (PostHog or simple flag-driven split) — `STATE.assign_variant(experiment_id, identity, variants)` is a pure-function, deterministic A/B/n helper: `SHA-256("<experiment_id>|<identity>")` → modulo `len(variants)` → return slug. Domain-separator (`experiment_id` in the hash prefix) means a single visitor gets independent assignments across different experiments — no "consistently lucky" skew. `STATE.experiment_config()` reads the operator-edited `experiments.json` (template at `deploy/experiments.json.example`). Slug validator rejects anything non-alnum/dash/underscore. Tests: `tests/test_phase7_ab_experiments.py` (7 tests, green) prove same-identity stability, cross-experiment independence, ~even split at 10K samples (within 5% of mean) for both 2-way and 3-way splits, and config validation. Live HTTP endpoint deferred until the apex marketing landing exists (#13 operator)
62. [x] SEO content cadence: 1 indexable job-alert page per major role × major city (10 cities × 20 roles = 200 pages) — `scripts/generate-seo-pages.py` fans out a small operator-edited config (cities + roles) into the 200-entry `seo-pages.json`. Engineering side of the page render itself is already shipped (#17). `deploy/seo-pages-input.json.example` ships with exactly 10 cities (Berlin / Munich / Hamburg / Frankfurt / Cologne / Stuttgart / Düsseldorf / Leipzig / Vienna / Zürich) × 20 roles → 200 pages, matching the tracker target. Re-running the script with the same input is a no-op (de-dupe on slug). Tests: `tests/test_phase7_seo_generator.py` (6 tests, green) cover full cross-product, template substitution, dedup, invalid-entry filtering, default template, and a shipped-input-yields-200 assertion that locks the spec target. Operator workflow: edit `seo-pages-input.json.example`, run `./scripts/generate-seo-pages.py`, copy the regenerated output to `<DATA_DIR>/seo-pages.json`
63. [x] LinkedIn / Twitter / Reddit content: weekly "lessons from the queue" posts with anonymized stats — four weeks × two posts each (LinkedIn long-form + Twitter / Mastodon thread) drafted in `docs/launch-day-content.md`: week 1 (operator's personal anti-LinkedIn story), week 2 (anonymized aggregate stats with consent-gated quotes), week 3 (skill-gap-atlas feature deep-dive), week 4 (first paid customer testimonial template). All voice-checked against `docs/brand-voice.md`. Operator pastes + publishes; the templates carry `[USER QUOTE]` placeholders for the testimonial
64. [x] Outreach: career coaches / bootcamps / university career centers — refer-a-cohort partnerships — four drop-in cold-email templates in `docs/launch-day-content.md`: career coach (refer-a-cohort partnership w/ kickback), coding bootcamp (free Pro for graduating cohorts), university career center (free Pro for current students), indie newsletter sponsorship (6-week run, 50/50 payment split). Each ties into the existing referral program (#46) — operator's referral code is the partner's tracking link
65. [x] Iterate on the differentiator features based on first 100 paying users' actual usage data — `docs/iteration-analytics.md` ships a 7-query SQL pack the operator runs against the prod SQLite database via `docker exec`: signup funnel (where users drop off), D1/D7/D30 retention by cohort, reply-rate distribution, top skill gaps across the queue, CV-variant attribution per index, day-3 drip effectiveness proxy, free→pro conversion. Plus a "what to NOT over-fit" guard against single-week noise + vanity metrics. Cadence: weekly first 90d, monthly thereafter. Engineering deliverable; the actual iteration is data-driven and happens once users exist

---

## Operator-only blockers (need decisions, not engineering)

66. [ ] Decide: stay SQLite or migrate to Postgres (gates Phase 0 #3, Phase 6 #58)
67. [ ] Decide: KMS provider — AWS KMS / GCP KMS / Vault (gates Phase 0 #2)
68. [ ] Choose pen-test vendor (gates Phase 0 #5)
69. [ ] Choose EU counsel for legal review (gates Phase 0 #8)
70. [ ] Register a legal entity if not yet done (sole proprietor / GmbH / Ltd) — required for ToS, invoices, Stripe payouts
71. [ ] Set up business bank account + Stripe Atlas / equivalent (operator-side)
72. [ ] Sign up Adzuna / Resend / Better Stack / DO Spaces under the legal entity once it exists

---

**Read this list as:** items 1–12 = ship-stoppers. Items 13–32 = launch-day must-have. Items 33–47 = the differentiator that makes us not-just-another-tracker. Items 48–65 = the long tail that makes us a business, not a project.

Done = reading this file shows every box ticked.

---

## Phase Q — Conversational OS (chat-router) — Round 12

73. [x] Backend chat-router framework — typed-command registry, multi-turn elicitation, confirmation gate, audit log (`company_discovery/chat_router.py`)
74. [x] 6 v1 commands wired end-to-end: `add_company`, `create_saved_search`, `find_jobs`, `update_profile`, `mark_applied`, `help`
75. [x] Slash-command parser (deterministic, AI-independent) — `/add`, `/find`, `/new-search`, `/profile`, `/applied`, `/help`
76. [x] Keyword intent router (regex over command synonyms) — fires before AI
77. [x] AI intent router (constrained: returns command-id only, fallback to keyword/help)
78. [x] Inline-arg parsing via `shlex` — quoted strings preserved (`/add "Acme Corp" https://acme.example`)
79. [x] Validators per param (URL, list/CSV, bool, application-status) — DE-friendly yes/no (ja/nein)
80. [x] Confirmation gate before every DB write — "yes/no/cancel/ja/nein" detection
81. [x] Per-command audit log via `STATE.log_analytics("chat_cmd", {...})`
82. [x] HTTP routes: `POST /api/chat/message`, `POST /api/chat/reset`, `GET /api/chat/state`, `GET /api/chat/commands`
83. [x] Chat UI in `static/index.html` + `static/app.js` — nav item, transcript bubbles, ARIA `role="log"`, keyboard-accessible
84. [x] 38 unit tests (`tests/test_chat_router.py`) — slash parser, keyword router, AI-response parser, validators, confirmation detection
85. [x] Multi-domain synthetic tester (`tests/e2e/multi_domain_tester.py`) — 10/10 PASS across backend, database, security, ai_router, data_analytics, product_ux, gdpr_audit, a11y_server, i18n, qa
86. [x] Documented migration path for remaining 25+ forms (deferred — chat handles CREATE/UPDATE, visual UI keeps BROWSE)

### Deferred — chat surface, additional commands

87. [x] Migrate remaining forms to chat commands — **partial: 10/15+ commands shipped.** Round 14 added: `tailor_cv`, `run_saved_search`, `set_persona`, `delete_company`. Still deferred: `import_job` (queue→imported promotion), `configure_ai_provider`, `schedule_watchlist`, `add_experience` (CV builder section adds), `bulk-actions`
88. [ ] Conversational CV builder (full chat wraps the existing wizard sections)
89. [ ] Voice input (Web Speech API) — same chat surface
90. [ ] Mobile-responsive chat layout (deferred backlog item — gates wider rollout)
91. [x] Chat-state persistence to `profile.chat_state` JSON column — **shipped, runtime-proven.** Session persists across server restart; verified on disk + in-memory via multi-domain `persistence` domain test

---

## Phase R — Honest closeout (Round 13)

92. [x] Chat UI driven through Playwright (`tests/e2e/chat_ui_agent.py` + `scripts/run-chat-ui-agent.sh`) — 10/10 PASS proving render, send-on-Enter, slash-with-inline-args, skip-optional, confirmation prompt, confirmation execution, reset button, keyword router via UI
93. [x] Bug #17 fixed — chat-UI swallowed whitespace messages (broke skip-optional path through the UI) — caught by the Playwright test
94. [x] `chat_cmd` audit log VERIFIED on disk — multi-domain tester now opens `company_discovery.sqlite3` directly, reads `analytics_events`, confirms 5 chat_cmd rows including the specific marker we sent
95. [x] Operator-side domains explicitly documented in `docs/deferred-test-backlog.md` (performance marketing, growth/SEO, CRO, brand, content strategist marketing-copy, email deliverability, social media) — rationale: they aren't coding tasks; need a separate operator-launch playbook
96. [x] Full regression sweep — 828 live checks across 8 e2e suites all green, zero regressions across 13 rounds
97. [x] AI router branch with real LLM call — **shipped, runtime-proven.** Round 15 wired `chat_ai_route` end-to-end with: (a) user-provider path (existing), (b) operator-managed fallback gated by `DIRECTJOB_CHAT_AI_ROUTER=true` + `DIRECTJOB_MANAGED_AI_KEY`, (c) 10-minute LRU cache (capped 1024 entries), (d) per-classification audit-log via `STATE.log_analytics("chat_ai_route", ...)`, (e) errors swallowed so a flaky LLM never breaks chat. 13 unit tests cover provider resolution, waterfall (user → managed → None), cache hit short-circuit, unknown-command rejection, hallucination guard.
98. [x] Operator-launch playbook starter shipped (`docs/operator-launch-playbook.md`) — covers all 7 business-side domains (performance marketing, growth/SEO, CRO, brand designer, content strategist, email deliverability, social media manager) with concrete operator checklists + a final launch-readiness gate

---

## Phase S — Round 14 additions (chat-router expansion + persistence)

99. [x] 4 new chat commands shipped: `tailor_cv`, `run_saved_search`, `set_persona`, `delete_company` — full handler + slash aliases + keyword routes + unit tests
100. [x] Bug #18 fixed — GET handler routes for `/api/chat/state` and `/api/chat/commands` referenced an undefined `data_user_id` (silently 500'd, caught by outer exception handler). Caught by the new `persistence` domain test
101. [x] Chat-state persistence to `profile.chat_state` — write-through on every chat message, rehydrate on first access in process. Pending commands survive across server restart
102. [x] Multi-domain tester extended to 12 domains (added `v2_commands` + `persistence`)
103. [x] Total live checks now 837 across 9 e2e suites, zero regressions

---

## Phase T — Round 15 (AI intent router + UI bug sweep)

104. [x] **Long-term AI intent router shipped** — `chat_ai_route` waterfall: user provider → operator-managed fallback (`DIRECTJOB_CHAT_AI_ROUTER=true` + `DIRECTJOB_MANAGED_AI_KEY`) → None. 10-min LRU cache (1024 entries). Per-classification audit-log. Errors swallowed. 13 unit tests pass.
105. [x] Bug #19 fixed — `/api/cv-builder/state` was POST-only but client called as GET → Python stdlib 404 with "File not found" body, cascade of broken header badge + stale photo error + section never rendering. GET handler added; route now returns 401 (auth required, exists) instead of 404.
106. [x] Bug #20 fixed — wizard dialog's `var(--surface-1)` token was never defined → background fell back to transparent → underlying view text bled through the modal. Switched to defined `--surface` token with explicit fallback; bumped backdrop to 75% + blur.
107. [x] Bug #21 fixed — CV Builder showed broken-image icon for empty/malformed photo URI. `renderCvBuilder` now only sets `img.src` when URI starts with `data:image/`; otherwise `removeAttribute("src")` + `display:none`.
108. [x] Bug #22 fixed — stale "Error: File not found" in photo status. `renderCvBuilder` clears `#cvBuilderPhotoStatus` on every render.
109. [x] Bug #23 fixed — CV Builder welcome state showed "All sections done" for never-started users. Now distinguishes "no state ever" (welcome) vs "everything done" (finish prompt).
110. [x] i18n parity restored — added `view.<id>.title/subtitle` keys to BOTH `en.json` and `de.json` so German-locale users no longer see "Discovered jobs" leaking through translated UI. 13 new keys per locale.
111. [x] Open-CV-builder navigation intent — new `open_cv_builder` chat command with NLP keywords for create/generate/build/make/write CV/resume/Lebenslauf in EN + DE; handler returns `navigateTo: "cvBuilder"`; client honours navigateTo by clicking the matching nav button. Fixes "I need you to generate a CV for me" falling through to help.
112. [x] Bug #18 already fixed in Round 13 — GET handler routes for `/api/chat/state` and `/api/chat/commands` referenced undefined `data_user_id` → silent 500s caught by outer exception handler. Same shape of bug as Bug #19.

---

## Phase U — 10-item closeout + Bug #25 (Round 15 continued, 2026-05-12)

The "fix 10 items 1 by 1 till 100%" plan. All ten items shipped; the
regression sweep caught one more bug (#25) at the end.

113. [x] **Item 1 — Long-term AI intent router** (see #104; user-provider → operator-managed fallback → None; 10-min LRU cache; per-user 20/60s rate limit; per-classification audit log; prompt-injection whitelist; 21 unit tests; real-LLM probe operator-runnable via `scripts/probe-ai-router.sh`)
114. [x] **Item 2 — JSON-output router prompt** — LLM now returns `{"command": "<name>", "args": {...}}`. `parse_ai_router_response` accepts both JSON and legacy bare-name. `parse_ai_router_extracted_args` returns the args dict (None values filtered). Chat handlers pre-fill slots from extracted args so the user gets confirmation immediately instead of re-stating fields.
115. [x] **Item 3 — `open_cv_builder` chat command** — NLP keywords for create/generate/build/make/write CV/resume/Lebenslauf in EN + DE; handler returns `navigateTo: "cvBuilder"`; client honors `navigateTo` by clicking the matching nav button. Fixes "I need you to generate a CV for me" falling through to help.
116. [x] **Item 4 — DSGVO disclosure for managed AI routing** — `static/privacy.html` section covers which message text is sent to the LLM, that no PII is forwarded, the audit log, operator's provider choice, and the user's right to disable.
117. [x] **Item 5 — Typing indicator** — `…` bubble during round-trip; replaced when the response arrives. `chatAppendBubble()` takes `opts.typing` and returns the bubble so callers can remove it.
118. [x] **Item 6 — Markdown in chat bubbles** — `chatRenderInline()` escapes then renders `**bold**` and `` `code` `` so help / error text renders readable instead of as literal asterisks/backticks.
119. [x] **Item 7 — Locale-aware view titles** — view titles resolve via `t('view.${view}.title', meta.title)`. 13 `view.<id>.title/subtitle` keys added to both `en.json` and `de.json` so the German UI no longer leaks English titles.
120. [x] **Item 8 — Mobile responsive layout** — `@media (max-width: 640px)` collapses CV builder grid to one column, expands chat input to full width with 16px font (prevents iOS auto-zoom), wizard fits inside the viewport. Mobile smoke `tests/e2e/mobile_smoke_agent.py` (Playwright 390×844) asserts each surface fits — 5/5 PASS.
121. [x] **Item 9 — Operator-side starters doc** — `docs/operator-starters.md` ships first-draft content for performance-marketing media plan, SEO keyword cluster, CRO experiments, brand identity, content sequence, email deliverability DNS, social media calendar. Companion to the playbook; operator red-pens rather than starting from blank page.
122. [x] **Item 10 — Real-LLM probe + script** — `tests/e2e/real_llm_probe.py` + `scripts/probe-ai-router.sh` — operator-runnable proof that the AI router classifies real NL inputs correctly (≥4/5 pass threshold). Not in the regression because it needs a real key + external network call.
123. [x] **Bug #25 — SW first-install reload race** — On first SW install, `controllerchange` fired and triggered `window.location.reload()`, racing with any in-flight user input. E2E tests intermittently failed because the SW activated mid-fill, the page reloaded, form values were lost, the click sent an empty payload, server returned `invalid_email`. Fixed by snapshotting `__hadInitialController = !!navigator.serviceWorker.controller` at load time and short-circuiting both `controllerchange` and `sw-updated` reloads when there was no prior controller. Cache version bumped to `v0.15.1`. Verified by running the chat-ui test 6× back-to-back against the same server — all green, zero double-submits.

### Final regression sweep — Round 15 / Phase U closeout

| Suite | Cases | Result |
|---|---:|---:|
| Unit tests | 698 | 698 / 698 |
| Multi-domain | 12 | 12 / 12 |
| CV Builder API e2e | 28 | 28 / 28 |
| CV Builder UI smoke | 6 | 6 / 6 |
| Mobile smoke | 5 | 5 / 5 |
| Chat UI smoke | 10 | 10 / 10 |
| Chaos API | 71 | 71 / 71 |
| Chaos UI XSS | 14 | 14 / 14 |
| Promise verifier | 8 | 8 / 8 |
| Synthetic 3-persona | 3 | 3 / 3 |
| **TOTAL live checks** | **855** | **855 / 855** |

Real-LLM probe is operator-runnable; excluded from automated
regression because it requires a real API key + external network.

---

## Phase V — Strict job-type filter + autonomous E2E verifier (Round 16, 2026-05-12)

User asked for two related deliverables: a NEW product feature that
strictly filters search by job type (e.g. "find me bartender jobs in
Berlin" returns only bar roles), and an AUTONOMOUS verifier agent
that runs the full A-to-Z flow and proves nothing is broken.

124. [x] **Strict job-type taxonomy** — `company_discovery/job_type_filter.py` ships an EN+DE synonym registry: bartender / Barkeeper / Schankkellner; barista / café staff; café_worker / Café-Mitarbeiter; pflegehelfer / Pflegeassistent / nursing assistant; waiter / Kellner. Each bucket holds the strict allow-list. `identify_bucket(text)` resolves a free-text role to a canonical key (longest-synonym wins); `job_matches_bucket(title, description, key)` is the word-boundary regex used to filter aggregator results.
125. [x] **`find_jobs` handler applies the strict filter** — when the user's query resolves to a known bucket, aggregator results are filtered to the bucket's synonyms BEFORE ranking. The user-facing reply states "Strict role filter applied — only this job type." When no bucket matches, behaviour is unchanged.
126. [x] **Watchlist auto-discovery honours the filter** — `service.scan_career_page` reads `profile.job_type_filter` + `profile.job_type_location_filter` and filters scan results before they hit the queue. Saved-search runs (`run_saved_search`) also honour these fields. User's most recent /find on a known role sets the profile-level filter automatically.
127. [x] **Natural-language extraction for the keyword router** — `extract_keyword_args(command_name, message)` pulls role + location from messages like "find bartender jobs in Berlin" / "Pflegehelfer in Deutschland gesucht" / "barista anywhere". Pre-fills the pending command so the user doesn't get re-asked.
128. [x] **Profile fields persist the filter** — `UserProfile.job_type_filter` + `job_type_location_filter` (both empty by default). `dataclass_from_payload` handles missing fields on old rows, so no migration is needed.
129. [x] **37 unit tests** — `tests/test_job_type_filter.py` covers synonym matching (EN+DE), word-boundary safety (no false positives), location normalization (Germany / Deutschland / anywhere / city), dict-shape filter for HTTP payloads, and keyword arg extraction for find_jobs.
130. [x] **Autonomous E2E verifier** — `tests/e2e/full_user_flow_agent.py` + `scripts/run-full-user-flow-agent.sh` — boots app, downloads a real JPEG from picsum.photos, registers, uploads the photo, drives the CV Builder through every section with DACH-norm dates (DD.MM.YYYY), navigates Chromium to /api/cv/print, strips the on-page toolbar (matches what the user gets via `window.print()` → "Save as PDF"), captures the PDF, saves it to `/Users/fouad./Desktop/directjob-scout-verification-CV.pdf`, parses the PDF with pypdf and asserts: non-empty, contains user's name, DACH dates present (≥2 TT.MM.JJJJ tokens), section order Summary→Experience→Education→Skills, photo embedded as data URI in source HTML, no AI-invented company names, no print-page chrome leaks. Then tests the new chat filter for `find bartender jobs in Berlin` AND `Pflegehelfer in Deutschland gesucht` — asserts each routes correctly, extracts the canonical role label, the canonical location, and the reply announces "Strict role filter applied".
131. [x] **22/22 PASS on first complete run** — PDF physically present at `/Users/fouad./Desktop/directjob-scout-verification-CV.pdf` (63,650 bytes); inspection confirms clean output, no toolbar leaks, all DACH-norm fields present.

### Final regression sweep — Round 16 / Phase V closeout

| Suite | Cases | Result |
|---|---:|---:|
| Unit tests | 735 | 735 / 735 |
| Multi-domain | 12 | 12 / 12 |
| CV Builder API e2e | 28 | 28 / 28 |
| CV Builder UI smoke | 6 | 6 / 6 |
| Mobile smoke | 5 | 5 / 5 |
| Chat UI smoke | 10 | 10 / 10 |
| Chaos API | 71 | 71 / 71 |
| Chaos UI XSS | 14 | 14 / 14 |
| Promise verifier | 8 | 8 / 8 |
| Synthetic 3-persona | 3 | 3 / 3 |
| **Full user flow (autonomous)** | **22** | **22 / 22** |
| **TOTAL live checks** | **914** | **914 / 914** |
